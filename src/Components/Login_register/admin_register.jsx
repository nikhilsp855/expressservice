import React from "react";

export class Adminregister extends React.Component {
    
        render(){
            return(
                <div>Only admin can add new admin</div>
            )
            
        }
}
